# Scaffolding endpoint (test_api) removed: it was csrf_exempt, unauthenticated,
# parsed request bodies without error handling, and was never routed in the
# project URLconf. No views are needed here.
